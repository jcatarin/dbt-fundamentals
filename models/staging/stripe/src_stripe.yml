version: 2

sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payment
        columns:
          - name: id
            description: Primary key for customers data.
            tests:
              - unique
              - not_null